name: flutter_integration_record
description: Generate Flutter integration test from record
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: 3.3.7

dependencies:
  bloc_test: ^9.1.0
  clock: ^1.1.1
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.1
  path_provider: ^2.0.11

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0
  very_good_analysis: ^3.1.0

flutter:
  uses-material-design: true
  generate: true